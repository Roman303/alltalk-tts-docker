{
  "train": {
    "epochs": 10,
    "batch_size": 2,
    "learning_rate": 0.0001,
    "warmup_steps": 100,
    "grad_accumulation_steps": 4,
    "save_every_epoch": true,
    "validation_split": 0.15,
    "early_stopping_patience": 3,
    "mixed_precision": true
  },
  "audio": {
    "sample_rate": 24000,
    "max_audio_length": 12
  },
  "notes": "Sanftes Training für 68 kurze 24kHz WAVs – ideal für ersten Testlauf."
}
